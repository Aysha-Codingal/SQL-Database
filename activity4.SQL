CREATE TABLE IF NOT EXISTS PRODUCT1(
PRO_ID TEXT PRIMARY KEY,
PRO_NAME TEXT,
PRO_PRICE INTEGER,
COMMISSION INTEGER
);

INSERT INTO PRODUCT1(PRO_ID,PRO_NAME,PRO_PRICE,COMMISSION)
VALUES
("101","MOTHER BOARD",3200,15),
("102","PRINTER",2600,13),
("103","MOUSE",250,12),
("104","KEYBOARD",450,14),
("105","SPEAKER",550,16);

DESC PRODUCT;

SELECT PRO_NAME, PRO_PRICE
    FROM PRODUCT1
    WHERE PRO_PRICE =
    (SELECT MIN(PRO_PRICE) FROM PRODUCT1);
    SELECT PRO_NAME, PRO_PRICE
    FROM PRODUCT1
    WHERE PRO_PRICE =
    (SELECT MAX(PRO_PRICE) FROM PRODUCT1);

