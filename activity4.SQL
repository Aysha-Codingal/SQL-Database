CREATE TABLE IF NOT EXISTS PRODUCT(
PRO_ID TEXT PRIMARY KEY,
PRO_NAME TEXT,
PRO_PRICE INTEGER,
COM INTEGER
);

INSERT INTO PRODUCT(PRO_ID,PRO_NAME,PRO_PRICE,COM)
VALUES
("101","MOTHER BOARD",3200,15),
("102","PRINTER",2600,13),
("103","MOUSE",250,12),
("104","KEYBOARD",450,14),
("105","SPEAKER",550,16);

DESC PRODUCT;

SELECT pro_name, Pro_price
    FROM PRODUCT
    WHERE pro_price
    (SELECT MIN(pro_price) FROM PRODUCT);
SELECT pro_name, Pro_price
    FROM PRODUCT
    WHERE pro_price
    (SELECT MAX(pro_price) FROM PRODUCT);

